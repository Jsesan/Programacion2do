#include "main.h"
float search1time(float, int)
float search1time(float *ptrV, int val)
{
    int i;
    for(i=0;i<;i++)
    {
        if(*(ptrV+i)== val)
        {
            return i;
        }

    }
    printf("El elemento solicitado no se encuentra en el array ");
    return (TAM+1);
}
